<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hino.dev.dashboardupdater">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MOList" />
        <activity android:name=".Login" />
        <activity android:name=".Scan" />
        <activity android:name=".MOPreview" />
        <activity android:name=".MaterialCall"></activity>
        <activity android:name=".SuccessTimeOut">

        </activity>
        <activity android:name=".ReturnToSection">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>